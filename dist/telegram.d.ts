import { TaskStatus, TelegramTask } from './task-store.js';
interface QueuedMessage {
    text: string;
    date: string;
}
export declare class TelegramBridge {
    private bot;
    private chatId;
    private taskStore;
    private pendingRequests;
    private optionsMap;
    private promptRequests;
    private pendingPhotoAlbums;
    private chatIdFile;
    private defaultTimeout;
    private activeProcesses;
    private geminiApiKey;
    private waitingForFileEdit;
    private pathIdMap;
    private pathIdCounter;
    constructor(token: string, options?: {
        chatId?: number;
        dataDir?: string;
        timeoutMs?: number;
    });
    private loadChatId;
    private saveChatId;
    private createIncomingTask;
    private findPendingResponseRequest;
    private resolveIncomingResponse;
    private resolvePendingRequest;
    private queuePhotoAlbum;
    private flushPhotoAlbum;
    private processPhotoUploads;
    private updatePhotoStatus;
    private upsertTaskStatus;
    private setupHandlers;
    private executeRemoteCommand;
    private sendMenu;
    private getStatusText;
    start(): Promise<void>;
    stop(): Promise<void>;
    get isReady(): boolean;
    notify(message: string): Promise<void>;
    confirm(question: string, timeoutMs?: number): Promise<boolean>;
    ask(question: string, options?: string[], timeoutMs?: number, taskId?: string, acceptFiles?: boolean): Promise<string>;
    getInbox(statuses?: TaskStatus[], limit?: number): TelegramTask[];
    getTask(taskId: string): TelegramTask;
    ackTasks(taskIds: string[]): TelegramTask[];
    updateTaskProgress(taskId: string, text: string, percent?: number): Promise<TelegramTask>;
    completeTask(taskId: string, summary: string, filePaths?: string[]): Promise<TelegramTask>;
    failTask(taskId: string, error: string, retryable: boolean): Promise<TelegramTask>;
    checkTaskCancellation(taskId: string, markCancelled?: boolean): TelegramTask;
    sendFile(filePath: string, caption?: string): Promise<void>;
    sendFiles(filePaths: string[], caption?: string): Promise<void>;
    takeScreenshot(url: string, caption?: string): Promise<string>;
    getMessages(): QueuedMessage[];
    private getPathId;
    private getPathFromId;
    private buildFileBrowserKeyboard;
    private sendFileBrowser;
    private editFileBrowser;
    private showFileMenu;
    private getMarkdownLanguage;
    private viewFileContent;
    private downloadTelegramFile;
    private ensureChatId;
    private waitForResponse;
    private uid;
}
export {};
