{
  "name": "aetherlink-telegram-mcp",
  "version": "2.2.2",
  "description": "AetherLink Telegram local stdio MCP plugin for Codex and ChatGPT Desktop",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test test/*.test.mjs",
    "start": "node start.mjs",
    "dev": "npx tsx src/index.ts"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "grammy": "^1.35.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.10.0",
    "tsx": "^4.19.0"
  }
}
